{% extends 'base.html.twig' %}

{% block title %}MainCategory{% endblock %}

{% block body %}
    <h1>MainCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ main_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ main_category.title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ main_category.createdAt ? main_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_category_index') }}">back to list</a>

    <a href="{{ path('main_category_edit', {'id': main_category.id}) }}">edit</a>

    {{ include('main_category/_delete_form.html.twig') }}
{% endblock %}
